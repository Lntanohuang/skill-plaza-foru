# 项目级协作约定

## `plaza-dev-notes` 开发手记

当任务与本项目的开发实现或设计决策有关时，必须把
`.agents/skills/plaza-dev-notes/SKILL.md` 作为项目上下文的一部分：

1. **开始前先读**：先读该 `SKILL.md`，再按任务范围读取其中“记录索引”指向的相关 `references/*.md`。适用范围包括但不限于 `web/server`、runner/pi/zcode、SSE/API 协议、报告解析或输出、运行记录、技能/引擎接入、前端运行历史，以及会影响这些边界的架构设计。
2. **过程中遵循记录**：把手记中的状态、schema、接入点、证据边界和后续路线当作当前项目约定；如果代码现状与记录冲突，先核对代码并在记录中反映实际情况。
3. **完成后持续写入**：本次工作形成新的设计决策、修改既有约定、实现一项记录中的路线，或发现会影响后续开发的重要事实时，必须在同一变更中更新对应记录，或新增下一个编号的 `references/<三位编号>-<slug>.md`，并更新 `SKILL.md` 的索引。实现落地后回填真实 `file:line`、验证结果和仍未完成的验收项。
4. **记录质量**：每条记录保持一个主题，并包含状态、动机、约定细节（含 schema/字段/接入点）、成本与取舍（含否决方案）和后续扩展路线；没有实测的内容标为待验证，不把计划写成已完成。

若任务只是与项目无关的通用操作，不需要读取或更新该 Skill；无法判断时按相关任务处理。
