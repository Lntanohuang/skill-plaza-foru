# SKILL 广场 · FORU 风格 Demo

一个无需 npm 的本地静态页面，以浅蓝白底、产物预览和资源卡片展示产教领域的 4 个公开 SKILL。

## 打开方式

下载仓库后，双击 `index.html`，用浏览器打开即可。

不需要联网、Node.js、安装依赖或启动服务器。本项目没有 `package.json`，无需执行 `npm install` 或 `npm run dev`。

## 功能

- 顶部菜单按院校管理者、教师、学生、数据团队分组，并直接进入对应 SKILL 详情。
- 产业专业、课程教学、实训就业、数据治理四类联动示意预览，可暂停自动展示。
- SKILL 名称、用途和标签搜索，分类筛选与排序。
- 详情页提供公开 GitHub 仓库、安装提示词和最小输入示例，并支持分别复制。
- hash 地址保存筛选条件，从详情返回可恢复列表状态。
- 桌面与手机响应式布局，减少动态效果适配。

## 文件

| 文件 | 用途 |
| --- | --- |
| `index.html` | 页面结构与近似 SVG 插画 |
| `styles.css` | 视觉样式与响应式布局 |
| `skills.js` | 4 个业务 SKILL 的展示资料、仓库、安装提示词与最小输入示例 |
| `app.js` | 导航、预览、检索、详情与复制 |
| `README.txt` | 本地使用说明与验证记录 |
| `skills/foru-web-ui/` | 完整 FORU SKILL，含说明、参考资料、原站证据截图和独立 starter |

## 附带的 FORU SKILL

完整技能位于 [skills/foru-web-ui](skills/foru-web-ui/)，入口为 [SKILL.md](skills/foru-web-ui/SKILL.md)。

下载仓库后，把整个 `foru-web-ui` 文件夹放入 Codex 的 skills 目录，保留 `agents`、`references` 和 `assets`，不要只复制 `SKILL.md`。

调用示例：

```text
使用 $foru-web-ui，为我的业务制作 FORU 风格的响应式页面。
保留原生 HTML/CSS/JS，明确角色入口、资源内容与必要交互。
```

技能素材、证据截图和 starter 按原目录完整保留。证据用于核对设计；复用时应使用自己的品牌与业务内容，不将原站客户、统计、联系方式或资质当作目标业务的背书。

## 演示边界

本页面复用 `foru-web-ui` 技能的独立 starter 设计语言和近似插画，不是 FORU 原站源码或截图，不使用原站 API、联系方式或合作背书。

预览是界面示意，不是实际运行结果。4 个业务条目均链接到公开 GitHub 仓库，但页面不在线执行 SKILL，也不提供账号系统；具体依赖、费用和许可以对应 SKILL 的发布资料为准。页面不生成推测日期、版本、评分或下载量。

## 验证

2026-09-15：目录更新为 4 个业务分类，每类 1 个 SKILL；4 个 GitHub 仓库均可公开读取。“从你的任务出发”角色切换区块已删除，顶部菜单改为直接进入 SKILL 详情。JavaScript 语法、分类数量、必填字段和 diff 空白检查通过，并在本地预览中验证首页渲染、分类筛选、详情渲染及安装提示词/最小输入示例的独立复制反馈。

2026-09-14：JavaScript 语法和本地资源引用检查通过。临时本地预览中检查了桌面、手机首页与详情、手机指南，以及角色切换、检索筛选、返回状态恢复、复制反馈、菜单和轮播暂停/恢复。

375、390、448、768、1024、1280、1560px 视口未出现页面横向溢出。减少动态效果做了静态检查，未切换系统偏好实测。浏览器验证工具限制 `file://` 访问，因此双击打开场景未直接实测；页面没有模块加载、fetch 或外部网络依赖。
